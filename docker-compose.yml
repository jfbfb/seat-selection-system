services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: seat_selection
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/data

volumes:
  postgres_data:
